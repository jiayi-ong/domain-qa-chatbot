----- STATEMENT ON THE USE OF GENERATIVE AI -----
ChatGPT 5.2 was used to support the completion of this assignment. It was used primarily to support the learning of software engineering best practices and optimal design. The base prompt requests for scaffolding assistance only - to generate the minimal template and function or class definitions with empty bodies. Components that were fully generated by ChatGPT are least related to the course, such as the frontend HTML code.

In-code citations are provided within the scripts that ChatGPT has contributed to.
The extent of ChatGPT's contribution to each script is classified into three categories:

1) Scaffold Assistance: ChatGPT generated only structural elements (e.g., file template, class definitions, function signatures, docstrings).

2) Guided Implementation: ChatGPT generated structural components and partial logic (core functions, method outlines, algorithm direction), including TODO markers or incomplete sections requiring that were completed manually.

3) AI-Drafted Implementation: The full implementation was initially generated by ChatGPT and subsequently reviewed, edited, optimized, or refactored manually.

All AI-generated code have been thoroughly reviewed, understood, and tested.

References:
OpenAI. (2025). ChatGPT (Version 5.2) [Large language model]. https://chat.openai.com/



----- Main prompt -----

The project requires me to build and deploy a domain Q&A chatbot for a narrow topic. Use FastAPI for the backend and a simple web UI for the frontend. The prompting strategy must include a clear role/persona, few-shot examples (at least 3), positive constraints, defined out-of-scope categories, and an escape hatch for uncertainty. Create 20+ test cases and a runnable eval script with deterministic metrics and MaaJ evals.

Recommend a structure for the project Python repository, including key scripts in each folder (concisely explain what each script does). Consider an object-oriented design where appropriate. E.g. class for prompts and evaluation metrics to facilitate future extension (but nothing too complex). Keep in mind all programming best practices and optimal design patterns. Note that this is still a project about Agentic AI, so don't go overboard with the networking components - only add the essentials. Lastly, this will be deployed to GCP Cloud Run.

Additional design considerations:
- I don't want singleton pattern for networking because it will serve many users at once
-  an agent.py to orchestrate: build prompt, call LLM (do I need a separate wrapper class on the base LLM call? if I want to store message history), run post-checks, return structured results
- I want a separate class for post-check: each subclass has a distinct deterministic trigger (e.g. regex match), a unique fallback prompt plus deterministic output string attached to the LLM's output from the fallback prompt. I think the highest level of this process can be included within agent.py too 
- call LLM with LiteLLM completion
- for a single session, store message history (growing context)
- folder structure: src/domain_qa/app,core,eval,ui

Provide only scaffolding assistance: generate only structural elements like file template, class definitions with empty bodies, function signatures, main docstrings.


----- Evaluation prompt -----

I built an LLM chatbot with FastAPI. I want to evaluate it against a golden dataset.
Recommend a Python repo structure, including key scripts in each folder (concisely explain what each script does). The purpose is to store the datasets (user query + golden response) and scripts to run the evaluation logic. Consider an object-oriented design where appropriate, but keep it simple. Lastly, a single windows / python command to run all evaluations and print results (and export text-file report).

Specific requirements:
1) for deterministic evaluation, compute the Jaccard score and ROUGE-N score (vs. the golden references)
2) for judge-golden reference, output a score between 0 and 10 (inclusive) - write the starter system prompt for me too
3) for judge-rubric: I want 10 evaluation criteria (rubric item), each one has a title, weight, and description. for your code, start with two. Example of rubric item: title - conciseness, description - does the answer contain redundant points or verbose language? weight - 10%
4) enforce JSON output schema for all evaluation output
5) the evaluation needs to run with one line of python/windows command 
6) the report should be simple but readable, exported to a location specified in the command

Provide only scaffolding assistance: generate only structural elements like file template, class definitions with empty bodies, function signatures, main docstrings.